import { defineConfig } from 'astro/config';

// Deployed to https://hackclubium.github.io/Braize/
// If a custom domain is added later, change `base` to '/'.
export default defineConfig({
  site: 'https://hackclubium.github.io',
  base: '/Braize',
});
